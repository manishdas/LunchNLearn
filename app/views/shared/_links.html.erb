<% if user_signed_in? %>
    <p>Welcome <%= current_user.email%> | <%= link_to "Sign Out", destroy_user_session_path%></p>
    <p>Votes Remaining :<span class ="votes_remaining"> <%= current_user.votes_remaining %></span></p>
<% end%>